Install
1. open terminal
2. go to the downloaded directory /*cd downloaded directory*/
3. npm install /*install from package.json*/
4. bower install /*install from bower.json*/
5. run gulp /*enter command 'gulp'*/
